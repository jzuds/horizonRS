resources:
  schemas:
    raw:
      name: raw
      catalog_name: osrs_analytics_${var.env_name}
      comment: ${var.env_name}
    analytics:
      name: analytics
      catalog_name: osrs_analytics_${var.env_name}
      comment: ${var.env_name}